# MEMORIA

**10-Noviembre-2025**

Hemos creado una parte de código que leía e imprimía línea por línea el dataset "personality.csv".
En dicho programa hemos implementado la librería **"fragmenta.h"** y la hemos modificado para adaptarse a nuestro programa principal **"programaPrincipal.c"**

Después hemos implementado el programa de **"maxMonticulo"** para que nos guarde las distancias y compare todos los elementos con todos y los inserte en una lista.
Comparamos los elementos y cogemos la mayor y asi vamos comparando uno a uno para saber cual es el numero mayor.
Si es menor la cantidad no haces nada, si es mayor lo insertas y borras y si es igual lo metes, para ello hicimos una tabla y los comparamos para quedarnos con los mejores valores llamada compFinal.

 
**17-Noviembre-2025**

En esta sesión hemos normalizado los datos del programa.
Hemos usado la fórmula: **v = (v-min) / (max_atributo - min_atributo)**
Además, hemos creado dos funciones llamadas **meterDatosStruc** para introducir los datos al programa para normalizarlo y ejecutarlo
y luego **imprimir** en el que imprimimos dichos resultados
Posteriormente hemos usado la libreria de **"listaOrdenada.h"** para crear un array que vaya comparando unos datos con cada elemento de la lista y así saber que usuario tiene una menor distancia con el elemento a comparar.
Hemos implementado una accion llamada **"insertarLista"** que mete los datos en la lista vacía. 
Implementamos una función que calcule la distancia mínima entre el dato elegido para que elija cual tiene la distancia menor y nos muestre cual es.
Hemos realizado hasta **K = 1 (primer entregable)**

